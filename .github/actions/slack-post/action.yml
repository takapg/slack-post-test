name: Slack post

inputs:
  text:
    required: true
    type: string

runs:
  using: "composite"
  steps:
    - name: Slack post
      run: |
        echo '
        <div style="border-left: 10px solid #FF5722;">
        ${{ inputs.text }}
        </div>' >> $GITHUB_STEP_SUMMARY
      shell: bash
