name: Slack post

inputs:
  text:
    required: true
    type: string

runs:
  using: "composite"
  steps:
    - name: Slack post
      run: |
        echo '
        <div>
        ${{ inputs.text }}
        </div>' >> $GITHUB_STEP_SUMMARY
      shell: bash
